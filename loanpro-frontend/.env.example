# Base URL for the LoanPro backend REST API.
# Copy this file to `.env` to override. When unset, the frontend defaults to
# http://localhost:8080/api (see src/api/apiClient.js).
VITE_API_URL=http://localhost:8080/api
